# CodeMentor AI - Frontend (client)

## Run frontend
cd client
npm install --legacy-peer-deps
npm run dev

The frontend will POST to the server URL defined in .env.local (defaults to http://localhost:4000).
